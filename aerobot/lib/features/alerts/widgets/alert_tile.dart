// Purpose: Alert Tile Widget
import 'package:flutter/material.dart';

class AlertTile extends StatelessWidget {
  const AlertTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(title: Text('Alert'));
  }
}
