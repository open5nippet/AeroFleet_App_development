// Purpose: Phone Detection Card Widget
import 'package:flutter/material.dart';

class PhoneDetectionCard extends StatelessWidget {
  const PhoneDetectionCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(child: Text('Phone Detection'));
  }
}
