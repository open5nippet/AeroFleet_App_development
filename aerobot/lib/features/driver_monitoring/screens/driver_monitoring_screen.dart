// Purpose: Driver Monitoring Screen
import 'package:flutter/material.dart';

class DriverMonitoringScreen extends StatelessWidget {
  const DriverMonitoringScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Driver Monitoring')),
      body: const Center(child: Text('Driver Monitoring Screen')),
    );
  }
}
